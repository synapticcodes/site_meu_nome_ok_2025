---
interface Props {
  items: readonly string[];
  title?: string;
}

const { items, title = 'O que você recebe' } = Astro.props as Props;
---
<section class="bg-neutral-50">
  <div class="mx-auto flex w-full max-w-5xl flex-col gap-6 px-6 py-12">
    <h2>{title}</h2>
    <ul class="grid gap-4 md:grid-cols-2">
      {items.map((item) => (
        <li class="flex items-start gap-3 rounded-3xl border border-neutral-100 bg-white p-5 shadow-sm">
          <span class="mt-1 flex h-6 w-6 items-center justify-center rounded-full bg-primary/10 text-primary">
            <svg class="h-3.5 w-3.5" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 10l3 3 7-7" />
            </svg>
          </span>
          <span class="text-neutral-600">{item}</span>
        </li>
      ))}
    </ul>
  </div>
</section>
