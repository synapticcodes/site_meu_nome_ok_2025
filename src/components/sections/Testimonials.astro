---
import TestimonialsCarousel from './TestimonialsCarousel';

type TestimonialsContent = typeof import('@content/home').testimonials;

interface Props {
  content: TestimonialsContent;
}

const { content } = Astro.props;
---
<section id="depoimentos" class="bg-white">
  <div class="relative isolate overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-br from-primary/12 via-white to-neutral-50" aria-hidden="true" />
    <div class="relative mx-auto flex w-full max-w-6xl flex-col gap-12 px-6 py-24">
      <div class="max-w-2xl space-y-4">
        <span class="text-xs font-semibold uppercase tracking-[0.3em] text-primary/80">Confiança</span>
        <h2 class="text-h2 text-neutral-900">{content.title}</h2>
        <p class="text-neutral-600">Resultados verdadeiros de pessoas e empresas que recuperaram seu crédito com o apoio da nossa equipe.</p>
      </div>
      <TestimonialsCarousel client:load items={content.items} />
    </div>
  </div>
</section>
