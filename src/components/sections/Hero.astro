---
import { Image } from 'astro:assets';
import heroIllustration from '@assets/images/hero.png';
import { RetroGrid } from '@components/ui/RetroGrid';

type HeroContent = typeof import('@content/home').hero;

interface HeroProps {
  content: HeroContent;
}

const { content } = Astro.props as HeroProps;
---
<section class="relative isolate overflow-hidden bg-gradient-to-b from-white via-primary/5 to-neutral-50">
  <div class="pointer-events-none absolute inset-0">
    <div class="absolute left-1/2 top-[-10%] h-[520px] w-[520px] -translate-x-1/2 rounded-full bg-primary/15 blur-[140px]" aria-hidden="true" />
    <div class="absolute -left-20 bottom-[-40%] h-[420px] w-[420px] rounded-full bg-primary/10 blur-[130px]" aria-hidden="true" />
    <RetroGrid client:load angle={68} cellSize={72} opacity={0.35} className="mix-blend-screen" />
  </div>
  <div class="relative mx-auto grid w-full max-w-6xl items-center gap-16 px-6 py-24 lg:grid-cols-[1.05fr_0.95fr]">
    <div class="space-y-8">
      <div class="inline-flex items-center gap-3 rounded-full border border-white/70 bg-white/80 px-5 py-2 text-sm font-medium text-primary shadow-[0_24px_60px_-30px_rgba(102,42,231,0.65)] backdrop-blur">
        <span class="flex h-2.5 w-2.5 rounded-full bg-emerald-400 shadow-[0_0_0_6px_rgba(16,185,129,0.18)]" aria-hidden="true"></span>
        {content.eyebrow}
        <span class="hidden rounded-full bg-primary/10 px-3 py-1 text-xs font-semibold uppercase tracking-wide text-primary/70 sm:inline-flex">
          Atendimento humano
        </span>
      </div>
      <div class="space-y-5">
        <h1 class="text-balance text-4xl font-semibold tracking-tight text-neutral-900 md:text-5xl lg:text-[3.35rem]">
          {content.title}
        </h1>
        <p class="max-w-xl text-lg leading-relaxed text-neutral-600 md:text-xl">
          {content.description}
        </p>
      </div>
      <div class="grid gap-4 text-sm text-neutral-600 sm:grid-cols-2">
        <div class="flex items-start gap-3 rounded-2xl border border-white/70 bg-white/80 p-4 shadow-sm shadow-primary/10 backdrop-blur">
          <span class="mt-1 flex h-9 w-9 items-center justify-center rounded-full bg-primary/10 text-primary">
            <svg class="h-4 w-4" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.7" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="m5 10 3 3L15 6" />
            </svg>
          </span>
          <p>Plano personalizado entregue em até 24 horas úteis.</p>
        </div>
        <div class="flex items-start gap-3 rounded-2xl border border-white/70 bg-white/80 p-4 shadow-sm shadow-primary/10 backdrop-blur">
          <span class="mt-1 flex h-9 w-9 items-center justify-center rounded-full bg-primary/10 text-primary">
            <svg class="h-4 w-4" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.7" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="m5 10 3 3L15 6" />
            </svg>
          </span>
          <p>Acompanhamento humano em cada negociação.</p>
        </div>
      </div>
      <div class="flex flex-col items-start gap-4 sm:flex-row">
        <a
          href={content.primaryCta.href}
          class="group inline-flex items-center justify-center rounded-full bg-gradient-to-r from-primary to-primary-dark px-9 py-3 text-base font-semibold text-white shadow-[0_26px_60px_-32px_rgba(102,42,231,0.9)] transition duration-200 hover:-translate-y-1 hover:shadow-[0_32px_70px_-32px_rgba(102,42,231,1)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"
        >
          <span class="flex items-center gap-2">
            {content.primaryCta.label}
            <svg class="h-4 w-4 transition-transform duration-200 group-hover:translate-x-1" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 10h10m0 0-4-4m4 4-4 4" />
            </svg>
          </span>
        </a>
        <a
          href={content.secondaryCta.href}
          class="inline-flex items-center justify-center rounded-full border border-primary/30 bg-white/80 px-9 py-3 text-base font-semibold text-primary shadow-sm shadow-primary/10 transition duration-200 hover:border-primary/60 hover:bg-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"
        >
          {content.secondaryCta.label}
        </a>
      </div>
      <dl class="grid w-full gap-4 sm:grid-cols-3">
        {content.highlights.map((item) => (
          <div class="group relative overflow-hidden rounded-3xl border border-white/70 bg-white/80 p-6 shadow-sm shadow-primary/10 backdrop-blur transition duration-200 hover:-translate-y-1 hover:shadow-[0_32px_70px_-32px_rgba(102,42,231,0.45)]">
            <span class="pointer-events-none absolute inset-x-5 top-0 h-px bg-gradient-to-r from-transparent via-primary/30 to-transparent" aria-hidden="true"></span>
            <dt class="text-sm font-medium text-neutral-500">{item.label}</dt>
            <dd class="mt-3 text-3xl font-semibold text-neutral-900">{item.value}</dd>
          </div>
        ))}
      </dl>
    </div>
    <div class="relative">
      <div class="absolute -top-16 right-12 h-48 w-48 rounded-full bg-primary/25 blur-[120px]" aria-hidden="true" />
      <div class="relative overflow-hidden rounded-[2.5rem] border border-white/70 bg-white/80 shadow-[0_45px_90px_-40px_rgba(102,42,231,0.45)] backdrop-blur">
        <div class="absolute inset-0 bg-gradient-to-br from-primary/10 via-transparent to-white/70" aria-hidden="true" />
        <Image
          src={heroIllustration}
          alt="Especialista do Meu Nome OK auxiliando cliente na renegociação de dívidas"
          width={680}
          height={680}
          class="h-full w-full object-cover"
          loading="eager"
          format="webp"
        />
        <div class="absolute inset-x-6 bottom-6 rounded-2xl border border-white/70 bg-white/85 p-5 shadow-lg shadow-primary/10 backdrop-blur">
          <p class="text-sm font-semibold text-neutral-900">Resultados reais em poucas etapas</p>
          <p class="mt-1 text-sm text-neutral-500">Monitoramos cada proposta até a conclusão do acordo com transparência total.</p>
        </div>
      </div>
    </div>
  </div>
</section>
