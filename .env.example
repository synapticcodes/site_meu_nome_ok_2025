# URL do webhook que receberá as submissões do formulário
FORM_WEBHOOK_URL=
# Endpoint público opcional para envio de Web Vitals (ex.: https://analytics.example.com/collect)
PUBLIC_ANALYTICS_ENDPOINT=
# Conexão com o banco de dados Supabase (use o connection string do pooler)
SUPABASE_DB_URL=
# Ajustes opcionais do pool (valores padrão: max=5 conexões, idle_timeout=30s)
SUPABASE_DB_POOL_MAX=
SUPABASE_DB_IDLE_TIMEOUT=
